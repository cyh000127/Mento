# https 인증서 발급 전 설정

server {
  listen 80;
  server_name i14a704.p.ssafy.io;

  # Let's Encrypt 인증용 경로
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }


  location / {
    return 200 "nginx http ok (stage 1)\n";
    add_header Content-Type text/plain;
  }
}
